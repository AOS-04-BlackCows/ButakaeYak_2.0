<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/feed_iv_thumb"
        android:layout_width="wrap_content"
        android:layout_height="80dp"
        android:minWidth="80dp"
        android:scaleType="fitCenter"
        android:src="@drawable/logo_final"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/kakao_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/medium"
        android:textColor="@color/gray"
        android:layout_marginStart="16dp"
        android:text="카카오톡 이름"
        app:layout_constraintStart_toEndOf="@+id/feed_iv_thumb"
        app:layout_constraintTop_toTopOf="@+id/feed_iv_thumb" />

    <TextView
        android:id="@+id/butakaeyak_nikename"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/medium"
        android:layout_marginTop="8dp"
        android:text="카카오톡 이름"
        app:layout_constraintStart_toStartOf="@+id/kakao_name"
        app:layout_constraintTop_toBottomOf="@+id/kakao_name" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/right_arrow"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>
</androidx.constraintlayout.widget.ConstraintLayout>